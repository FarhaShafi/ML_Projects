Clustering subfolder contains jupyter notebook file which implememts the preprocessing techniques and some of the Clustering algorithms.

The objective of this is to evaluate your understanding and ability to apply clustering
techniques to a real-world dataset.

# Dataset:
Uses the Iris dataset available in the sklearn library.

# Steps Involved:
  ### 1. Loading and Preprocessing
          - Loads the Iris dataset from sklearn.
          - Drops the species column since this is a clustering problem.
  ### 2. Clustering Algorithm Implementation
      Implement the following two clustering algorithms:
  ######   KMeans Clustering
          - Provides a brief description of how KMeans clustering works.
          - Explains why KMeans clustering might be suitable for the Iris dataset.
          - Applies KMeans clustering to the preprocessed Iris dataset and visualizes the clusters.
  ######   Hierarchical Clustering
          - Provides a brief description of how Hierarchical clustering works.
          - Explains why Hierarchical clustering might be suitable for the Iris dataset.
          - Applies Hierarchical clustering to the preprocessed Iris dataset and visualizes the clusters.
